# Scan configurations

- `scan_config.example.json`: documented starting template.
- `scan_config_both_clean_25mV.json`: matched Triangle/Star scan after trigger validation.
- `scan_config_ch3_25mV.json`: corrected PCB CH3 scan.

Configuration files preserve the settings used by the acquisition program, but the run manifest is the authoritative record for a completed scan.
